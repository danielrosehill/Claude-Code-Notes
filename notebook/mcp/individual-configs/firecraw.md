# Firecrawl MCP  

## User Level

```
claude mcp add firecrawl --scope user -e FIRECRAWL_API_KEY=yourkey -- npx -y firecrawl-mcp
```

## Project Level

```
 claude mcp add firecrawl-mcp npx firecrawl-mcp --env FIRECRAWL_API_KEY=yourapikey
 ```