Update the index, commit all changes, and push to remote.

## Instructions

1. First, run the update-index logic: scan all `notes/*.md` files and rebuild the Notes Index table in README.md (sorted newest first).
2. Stage all changed/new files in `notes/` and `README.md`.
3. Create a commit with a message describing what notes were added or changed.
4. Push to the remote.
5. Report what was published.
